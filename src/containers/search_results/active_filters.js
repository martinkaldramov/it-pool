// @flow

import React from 'react';

type filtersTypes = {
  name: string,
  categorie: string,
  active: boolean,
};

const ActiveFilters = props => <p>Active filters</p>;

export default ActiveFilters;
